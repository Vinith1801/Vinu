// PlayerScreen.tsx
import React from 'react';
import { View, Text } from 'react-native';

const PlayerScreen = () => {
  return (
    <View className="flex-1 justify-center items-center bg-black">
      <Text className="text-white text-lg">Player Screen</Text>
    </View>
  );
};

export default PlayerScreen;
